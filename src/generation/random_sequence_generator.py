
import time
from . import AbstractGenerator


class MusicVAEGenerator(AbstractGenerator):
    """
    docstring
    """

    def __init__(self, ):
        super().__init__()
        
        print('[GEN] 🎉 Random generator initialized')


    def generate(self, length_in_quarters=8, octave_range=2, max_beat_division=24, time_signature=(4, 4), tempo=120):
        t1 = time.time()
        # generate
        t2 = time.time()

        

    def generateMultiple(self, number=10, length_in_quarters=8, octave_range=2, max_beat_division=24, time_signature=(4, 4), tempo=120):
        t1 = time.time()
        # generate
        t2 = time.time()

        
